# Optional: GitHub Personal Access Token for higher rate limits (5000 req/hr vs 60 req/hr)
# GITHUB_TOKEN=ghp_your_token_here

# Transport mode: "stdio" (default) or "http"
# TRANSPORT=stdio

# HTTP port (only used when TRANSPORT=http)
# PORT=3000
